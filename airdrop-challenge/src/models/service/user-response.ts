export interface UserResponse {
  address: string;
  email: string;
  verified_email: boolean;
  raw_score: number;
  total_score: number;
  rank: number;
  referral_code: string;
  referral_count: number;
}
